#!/usr/bin/python
#Vifm wrapper
import os, sys

file = sys.argv[1]
ext = file.split('.')[-1:][0]

if ext == 'avi' or ext == 'mkv':
    os.system('smplayer "%s"' % file)
elif ext == 'mp4':
    os.system('xine "%s"' % file)
else:
    os.system('vim "%s"' % file)
