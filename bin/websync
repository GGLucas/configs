#!/bin/bash
source ~/.rsync_locs
for name in $@; do
    eval "r=\$RSYNC_$name"
    echo Syncing $name
    echo "rsync -tavz --progress $r"
    echo --------------------
    eval "rsync -tavz --progress $r"
done
