#!/bin/bash
if [[ $(mpc current) =~ "AnimeNfo" ]];
then
    notify-send 'Now Playing' "$(mpc current | sed -e 's/^.*\!: //' -e 's/\"//g' -e 's/&/&amp;/g')"
else
    notify-send 'Now Playing' "$(mpc | head -n 2 | sed -e 's/\"//g' -e 's/&/&amp;/g')"
fi
