#!/bin/bash
if [[ -d "/data/others/dw" ]]
then

echo "Unmounting"
sudo umount /data/others/dw
truecrypt -t -d /mnt/data-3/virtual/crypt.img
rmdir /data/others/dw

else

echo "Mounting"
mkdir /data/others/dw
sudo truecrypt -t /mnt/data-3/virtual/crypt.img /data/others/dw

fi
