#!/bin/bash
for a in *.zip; do
    o=$(basename "$a" .zip); 
    mkdir "$o"; 
    mv "$a" "$o"/; 
    cd "$o"/; 
    unzip "$a"; 

    if [[ $(ls | wc -w) -eq 1 ]]; then
        mv */* .
    fi;


    cd ..;
done;
