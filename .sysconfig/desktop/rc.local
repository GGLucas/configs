#!/bin/bash
#
# /etc/rc.local: Local multi-user startup script.
#

# Remove useless modules
modprobe -r saa7134_alsa
modprobe -r pcspkr

# Modprobe loop if it exists
modprobe loop 2> /dev/null

# Map caps to escape
(echo `dumpkeys |grep -i keymaps`; echo keycode 58 = Escape) |loadkeys 

